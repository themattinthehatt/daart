# name of directory containing input data: 'markers' | 'features' | ...
input_type: markers

# number of classes (including background)
output_size: 6

# experiment ids
expt_ids:
    - 2019_06_26_fly2
    - 2019_08_07_fly2

# base data directory
data_dir: /path/to/data

# base results directory
results_dir: /path/to/results
